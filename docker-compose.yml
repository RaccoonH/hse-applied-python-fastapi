services:
  redis:
    image: redis:alpine
    container_name: redis

  app:
    build: .
    container_name: hse_fast_api_app
    ports:
      - 8000:8000
    depends_on:
      - redis
    command: ["/fastapi_app/run_app.sh"]
